<script lang="ts">
  // No props: the obligations are fixed at build time, not passed in.
</script>

<!-- Two licence obligations ship with the app: Ultralytics is AGPL-3.0 and
     the evermeet.cx ffmpeg builds are GPL. The public repository satisfies
     the source obligation; this names them somewhere a user can find them,
     which is the part a repo link alone does not do. -->
<div class="mt-3 border-t border-line pt-3">
  <span class="text-caption text-dim">Open source</span>
  <span class="mt-1 block text-caption text-faint">
    Detection by Ultralytics YOLO11 (AGPL-3.0). Video by FFmpeg (GPL).
    Source at github.com/stevenkhaw/SplitStep.
  </span>
</div>
